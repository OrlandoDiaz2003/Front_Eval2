services:
  front:
    container_name: frontend_usuarios
    restart: always
    build: .
    ports:
      - "${PORT}:5000"
    environment:
      - BACKEND_URL=${BACKEND_URL}
      - SECRET_KEY=${SECRET_KEY}
